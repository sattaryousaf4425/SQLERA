'use client'
import {FormEvent,useEffect,useState} from 'react'
import {useRouter,useSearchParams} from 'next/navigation'
import {supabase} from '@/lib/supabase'
export default function AuthForm(){const params=useSearchParams(),router=useRouter();const [signup,setSignup]=useState(params.get('mode')==='signup');const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState('');const [message,setMessage]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>{if(data.session)router.replace('/dashboard')})},[router]);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');setMessage('');if(signup){const {error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name},emailRedirectTo:location.origin+'/dashboard'}});if(error)setError(error.message);else setMessage('Check your email to verify your SQLERA account.')}else{const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);else router.push('/dashboard')}setLoading(false)}async function forgot(){if(!email)return setError('Enter your email first.');const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/reset-password'});if(error)setError(error.message);else setMessage('Password reset email sent.')}return <div className="card"><a className="brand" href="/"><span className="mark">SQL</span>SQLERA</a><h1>{signup?'Create your account':'Welcome back'}</h1><p className="muted">{signup?'Join the free public beta.':'Continue your SQL interview preparation.'}</p>{error&&<div className="error">{error}</div>}{message&&<div className="success">{message}</div>}<form className="form" onSubmit={submit}>{signup&&<div className="field"><label>Full name</label><input value={name} onChange={e=>setName(e.target.value)} required/></div>}<div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Password</label><input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn primary" disabled={loading}>{loading?'Please wait…':signup?'Create free account':'Sign in'}</button></form>{!signup&&<p className="switch"><button onClick={forgot}>Forgot password?</button></p>}<p className="switch">{signup?'Already registered?':'New to SQLERA?'} <button onClick={()=>{setSignup(!signup);setError('');setMessage('')}}>{signup?'Sign in':'Create an account'}</button></p></div>}
